# Teste prático Admin + Dados

Abra `admin.html` para alterar plano, benefícios e Exchange.
Depois abra `index.html` e entre em Antes.

Nesta demo, os dados ficam em localStorage no mesmo navegador.
É a etapa de validação antes de ligarmos o Firebase real.
